SILENT = "silent"
SOFT = "soft"
BALANCED = "balanced"
STRICT = "strict"
AGGRESSIVE = "aggressive"

DEFAULT_PERSONA = BALANCED

ALL_PERSONAS = {
    SILENT,
    SOFT,
    BALANCED,
    STRICT,
    AGGRESSIVE,
}